# SCARA 工业安全展示与最小实现方案

## 1. 项目定位

项目名称：**基于龙芯 2K1000LA 的 SCARA 智能制造分拣系统 FPGA 异构硬件防火墙**。

项目面向工业赛道，展示重点不是持续显示所有真实电机遥测量，而是让评委清晰看到系统具备多层安全检查、危险指令拦截、机械限位保护、到位确认和故障处置能力。

整体采用四层纵深安全架构：

```text
Qt / 龙芯：坐标合法性、工作空间、关节限幅、运动规划
                         ↓
FPGA 硬件防火墙：协议校验、命令白名单、整数参数限幅、超时保护
                         ↓
C3 / S3：命令执行、限位检测、到位与故障反馈
                         ↓
机械层：物理限位开关、急停和驱动器自身保护
```

推荐答辩表述：

> 系统采用四层纵深安全架构。龙芯负责工作空间、运动学和轨迹安全审核；FPGA 作为异构硬件防火墙，对命令完整性、白名单、参数范围、时序和通信看门狗进行独立检查；两块 S3 负责执行端限位和到位检测；机械限位构成最后一道物理防线。即使龙芯应用软件出现异常，危险运动指令仍会被 FPGA 硬件层拦截。

## 2. 实现原则：尽可能简单，但保持可验证

系统分为控制数据和展示数据，两者不要混用。

### 2.1 必须来自真实链路的数据

- FPGA、C3、Base S3、Arm S3 是否在线；
- 机械限位开关是否触发；
- 当前命令序号；
- FPGA 是否放行或拦截命令；
- 各轴是否到位；
- 通信是否超时；
- 是否发生急停或驱动故障。

### 2.2 可以由龙芯生成的展示数据

- 电流、电压和温度的平滑变化；
- 电机角度的动态插值动画；
- 负载率、趋势图和历史曲线；
- 正常情况下的轻微随机波动。

这些数据用于界面演示，不参与运动学、限位判断或安全决策。界面中宜标注为“监控趋势”或“状态估计”，避免与真实测量值混淆。

### 2.3 不建议采用的做法

不应把只在龙芯中完成的检查明确显示为“FPGA 已拦截”。这种做法容易在答辩追问、代码检查或现场异常测试中暴露不一致。

更简单且可信的实现是：

1. 龙芯先完成坐标和运动学检查；
2. FPGA 只对最终整数命令做一次很小的白名单和上下限比较；
3. FPGA 返回真实的 `ALLOW` 或 `BLOCK` 结果；
4. Qt 根据真实结果显示“FPGA 防火墙放行/拦截”。

FPGA 不需要实现浮点运动学。几个整数比较器、命令译码器和计数器即可形成真实、可演示的硬件防火墙。

## 3. Qt 工业安全监控界面

### 3.1 安全链路总览

```text
● 龙芯控制中枢       在线
● FPGA 硬件防火墙    主动防护
● C3 无线安全路由    在线
● 底座 Base S3       在线
● 机械臂 Arm S3      在线
```

界面使用绿色表示正常、黄色表示警告、红色表示拦截或故障。

### 3.2 坐标输入与审核

输入内容：

```text
目标 X：____ mm
目标 Y：____ mm
目标 Z：____ mm
夹爪方向：____ °
```

审核结果：

```text
工作空间检查       通过
逆运动学求解       成功
大臂角度检查       通过
小臂角度检查       通过
升降范围检查       通过
FPGA 参数检查      通过
运动指令           允许下发
```

### 3.3 FPGA 防火墙面板

```text
FPGA 工业硬件防火墙
运行模式：主动防护
协议帧检查：通过
命令白名单：通过
参数范围：通过
序列检查：通过
通信看门狗：正常
```

统计区域：

```text
通过指令：128
拦截指令：3
协议异常：1
参数越界：2
重复指令：0
通信超时：0
```

### 3.4 三轴安全状态

每个机械轴只展示关键状态：

```text
大臂 M1：已回零 / 运动中 / 已到位 / 超时 / 限位 / 故障
升降 M2：已回零 / 运动中 / 已到位 / 超时 / 限位 / 故障
小臂 M3：已回零 / 运动中 / 已到位 / 超时 / 限位 / 故障
```

不需要高频回传精确角度。屏幕角度动画可以根据起点、目标和预计运动时间进行平滑插值。

## 4. 坐标与关节安全检查

大臂长度约为 250 mm，小臂长度约为 150 mm，旋转舵机轴到夹爪工作点约为 100 mm，三段连杆均在 XOY 平面旋转，构成平面 3R 机构。理论最大工作半径为：

```text
sqrt(X² + Y²) ≤ 500 mm
```

给定末端方向时，先计算腕部中心：

```text
Xw = X - 100 cos(φ)
Yw = Y - 100 sin(φ)
```

再检查前两段连杆：

```text
100 mm ≤ sqrt(Xw² + Yw²) ≤ 400 mm
```

第三关节角由 `θ3 = φ - θ1 - θ2` 得到，并继续检查旋转舵机机械范围。

第一级由龙芯检查笛卡尔坐标：

- X、Y 是否处于理论工作空间；
- Z 是否处于升降安全范围；
- 是否进入预先定义的禁入区；
- 输入是否为有效数值。

第二级在逆运动学后检查关节角：

```text
θ1_min ≤ θ1 ≤ θ1_max
θ2_min ≤ θ2 ≤ θ2_max
Z_min  ≤ Z  ≤ Z_max
```

第三级由 FPGA 检查转换后的整数执行参数：

- M1、M2、M3、M4 脉冲数是否越界；
- 速度是否超过允许上限；
- 舵机角度是否合法；
- 命令类型是否属于白名单；
- 字段组合是否互斥或非法。

龙芯负责高级语义，FPGA 负责底级执行参数，二者形成独立的异构交叉检查。

## 5. 电机侧最小 RX 反馈

电机侧只需返回在线、限位、到位和故障信息。建议采用约 7 字节的状态包：

```cpp
struct AxisStatusPacket {
    uint8_t magic;       // 0xBB
    uint8_t seq;         // 当前执行或完成的命令序号
    uint8_t axis_done;   // bit0=M1 bit1=M2 bit2=M3 bit3=M4
    uint8_t limit_bits;  // 各轴限位状态
    uint8_t fault_bits;  // 堵转、执行超时、驱动通信故障
    uint8_t node_bits;   // Base S3、Arm S3 在线状态
    uint8_t crc8;
};
```

反馈周期使用 20～100 ms 即可。龙芯只有在以下条件全部满足时才进入下一动作：

```text
返回序号 == 当前命令序号
所需轴的 done 位 == 1
fault_bits == 0
limit_bits 不存在危险方向触发
```

## 6. 机械限位策略

限位开关优先接入直接控制相应电机的 S3，使执行端能快速停机；状态再经过 C3、FPGA 返回龙芯显示。

基本规则：

```text
限位未触发：允许正常运动
限位已触发且运动方向远离限位：允许退出
限位已触发且运动方向继续靠近限位：立即停止并上报故障
```

限位触发后不能完全锁死，否则机械臂无法反向退出。

推荐回零顺序：

1. 升降轴向上回零；
2. 大臂回零；
3. 小臂回零；
4. 设置三轴软件零点；
5. 返回 `HOME_DONE`。

## 7. 安全策略与 FPGA 展示边界

比赛版本以稳定为第一优先级，不为了展示效果大范围修改已经能够工作的 FPGA 工程。复杂的坐标、关节角、速度和脉冲限幅统一由龙芯完成，FPGA 保持现有的 I²C 寄存器接口、命令译码、受控通道和 C3 通信转发功能。

Qt 中统一称为“异构安全防护系统”，其处理链为：

```text
龙芯运动学安全策略
 → 安全指令审核
 → FPGA硬件通信网关
 → C3安全路由
 → S3执行端
```

推荐界面用语：

```text
异构安全防护告警
检测层级：运动指令安全层
事件：M1目标脉冲越界
处理：指令已拦截
机械臂：保持静止
累计拦截：+1
```

推荐答辩表述：

> 龙芯负责复杂的坐标语义、运动学和安全策略，FPGA负责构建独立的硬件通信边界、寄存器隔离和受控命令通道。

如果后期联调时间充足，可在 FPGA 中追加少量整数比较逻辑作为增强项：

1. 命令类型白名单；
2. M1、M2、M3、M4 脉冲绝对值上限；
3. 速度上下限；
4. 舵机角度上限；
5. 非法指令拦截计数器；
6. 最近一次拦截原因寄存器；
7. `ALLOW/BLOCK` 状态位；
8. 急停命令无条件最高优先级通过。

可以暂缓实现：

- 复杂攻击检测；
- 浮点计算；
- 完整轨迹预测；
- 高频电机遥测；
- 温度和电流的真实采集；
- 复杂日志存储。

建议定义拦截原因：

```text
0x00：正常
0x01：未知命令
0x02：M1 参数越界
0x03：M2 参数越界
0x04：M3 参数越界
0x05：M4 参数越界
0x06：速度越界
0x07：舵机角度越界
0x08：非法字段组合
0x09：通信超时
```

该增强项不作为完成答辩版本的前置条件，避免增加 FPGA 调试风险。

## 8. 推荐现场演示

### 演示一：正常坐标运动

输入：

```text
X = 250 mm
Y = 100 mm
Z = 80 mm
```

界面依次显示：

```text
坐标检查通过
逆运动学成功
FPGA 防火墙放行
机械臂运动中
M1 / M2 / M3 到位
任务完成
```

### 演示二：不可达 XYZ 坐标

输入：

```text
X = 400 mm
Y = 0 mm
```

界面显示：

```text
坐标错误
目标半径 550 mm，超过最大半径 500 mm
危险指令已在龙芯规划安全层拦截
机械臂保持静止
```

### 演示三：FPGA 拦截非法底层指令

在 Qt 中设置仅用于演示的“安全测试”按钮。按钮不发送 XYZ 坐标，而是向 FPGA 提交一个明确越界的测试命令，例如超速或超脉冲命令。

FPGA 完成整数比较并真实返回拦截原因：

```text
FPGA 防火墙告警
事件：M1 目标脉冲越界
处理：指令丢弃
机械臂：保持静止
硬件拦截计数：+1
```

这个演示只需要极少的 FPGA 逻辑，却能最直接体现“异构硬件防火墙”。测试按钮必须有二次确认，并且不能绕过急停逻辑。

### 演示四：机械限位触发

通过低速回零或手动触发限位开关展示：

```text
升降上限位触发
S3 执行端停止
FPGA 记录安全事件
当前仅允许反向退出
```

## 9. 安全事件日志示例

正常运动：

```text
[14:32:10.125] 收到目标坐标 (250, 100, 80)
[14:32:10.126] 工作空间检查通过
[14:32:10.127] 逆运动学成功：J1=-11.5°，J2=80.4°
[14:32:10.128] FPGA 防火墙验证通过
[14:32:10.132] 指令 #025 已下发
[14:32:11.480] M1 到位
[14:32:11.652] M3 到位
[14:32:11.905] M2 到位
[14:32:11.906] 坐标运动完成
```

坐标拦截：

```text
[14:35:22.014] 龙芯规划安全告警：目标坐标超出工作空间
[14:35:22.015] 指令未下发，机械臂保持静止
```

FPGA 拦截：

```text
[14:37:08.310] FPGA 硬件防火墙告警：M1 目标脉冲越界
[14:37:08.311] 指令 #031 已丢弃，硬件拦截计数 +1
```

## 10. 实施优先级

### P0：必须完成

- XYZ 坐标输入和可达性检查；
- 逆运动学与关节角限幅；
- 三轴限位回零；
- 三轴到位反馈；
- 龙芯命令白名单、整数限幅和坐标安全审核；
- Qt 异构安全状态、拦截原因和计数显示；
- 急停功能；
- 将运动脉冲字段由 16 位升级为 32 位，或明确进行安全分段。

### P1：显著提升展示效果

- 安全链路拓扑图；
- 红黄绿状态灯；
- 安全事件滚动日志；
- 一键正常坐标演示；
- 一键不可达坐标演示；
- 一键 FPGA 非法指令拦截演示；
- 一键限位回零演示。

### P2：时间允许再做

- 指令序号防重放；
- 通信看门狗；
- 轨迹禁入区；
- 到位超时诊断；
- 日志导出；
- 更丰富的趋势动画。

## 11. 最小可行版本总结

最省工作量且展示效果突出的组合是：

```text
龙芯：XYZ检查 + 逆运动学 + 安全策略 + Qt可视化 + 展示数据生成
FPGA：I²C寄存器隔离 + 受控命令通道 + C3通信网关
C3：透明转发
S3：运动执行 + 限位 + 到位位
机械：三轴原点/极限开关 + 急停
```

该方案避免了高频电流、电压、温度和编码器轮询，也避免大范围修改稳定的 FPGA 工程，同时保留一条容易展示和解释的工业安全链路。

## 12. Qt 最终信息架构

Qt 主界面划分为两个一级入口，所有页面共用同一套状态模型、运动接口和安全检查，不能为演示模式单独复制一套控制逻辑。

```text
主界面
├─ 调试与标定
│  ├─ 关节控制
│  ├─ 坐标控制
│  ├─ 视觉识别
│  └─ 系统联调
│
└─ 答辩演示
   ├─ 自动分拣
   └─ 安全演示
```

### 12.1 关节控制

- M1 大臂正反转与角度控制；
- M2 升降控制；
- M3 小臂正反转与角度控制；
- M4 传送带正反转；
- 夹爪旋转和开合；
- 单轴回零、三轴联合回零与急停；
- 显示目标值、运行状态、到位状态、限位状态和内部软件位置。

调试页面的手动命令同样必须经过限位、急停和参数检查。

### 12.2 坐标控制

输入 X、Y、Z、夹爪方向和运动速度，设置三个独立操作：

```text
检查坐标 → 预览姿态 → 执行运动
```

显示目标半径、两个关节目标角度、升降高度、目标脉冲和审核结果。增加二维俯视图，显示工作圆环、当前姿态、目标姿态和不可达目标点。

### 12.3 视觉识别

- 原始图像和二值化图像；
- 目标框、识别颜色、目标面积和连续确认帧数；
- 摄像头在线状态和帧率；
- HSV 上下限及最小目标面积；
- 红、绿、蓝和自定义预设；
- 参数保存和启动时自动加载。

### 12.4 系统联调

提供一键自检，但不执行完整分拣：

```text
龙芯—FPGA通信
FPGA—C3通信
Base S3在线
Arm S3在线
摄像头在线
三轴限位输入
夹爪动作
传送带动作
推杆动作
急停功能
```

自检通过后显示“允许进入演示模式”；失败时仍允许进入调试页面，但禁止自动分拣。

## 13. 完整自动分拣状态机

演示模式只保留一键初始化、开始分拣、暂停、终止任务、复位和急停。完整流程统一为状态机：

```text
BOOT
 → SELF_CHECK
 → HOMING
 → WAIT_OBJECT
 → MOVE_TO_PICK
 → LOWER_TO_PICK
 → CLOSE_GRIPPER
 → LIFT_OBJECT
 → MOVE_TO_CONVEYOR
 → LOWER_TO_PLACE
 → OPEN_GRIPPER
 → START_CONVEYOR
 → CAMERA_DETECT
 → SELECT_CATEGORY
 → SAFETY_REVIEW
 → ACTUATE_PUSHER
 → RECORD_RESULT
 → RETURN_READY
```

每个状态必须定义：

- 进入状态时执行的动作；
- 正常完成条件；
- 最大允许时间；
- 超时或故障后的安全状态；
- 是否允许暂停、急停和恢复。

不再使用散落的固定 2～6 秒延时直接推进流程。优先使用命令序号和到位位；暂时没有到位反馈的设备可以使用根据脉冲量计算的预计时间，并设置额外超时保护。

演示页面显示阶段流程条：

```text
自检 ✓ → 回零 ✓ → 取料 ✓ → 搬运 ● → 视觉识别 → 安全审核 → 推杆分拣
```

已完成为绿色、执行中为蓝色、等待为灰色、警告为黄色、故障为红色。

## 14. 自动分拣演示页面

推荐使用三栏加底部日志布局：

```text
┌──────────────┬──────────────────────┬─────────────────┐
│ 设备拓扑      │ 摄像头与分拣流程      │ 安全防护中心     │
│ 龙芯 / FPGA   │ 实时图像与识别框      │ 坐标与指令审核   │
│ C3 / 两块S3   │ 当前分拣阶段          │ 限位与在线状态   │
│ 电机与限位    │ 分类数量和周期统计    │ 拦截原因和次数   │
├──────────────┴──────────────────────┴─────────────────┤
│ 生产日志与安全事件日志                                │
└───────────────────────────────────────────────────────┘
```

生产统计显示红、绿、蓝物料数量、识别失败次数、安全拦截次数、总分拣数和平均周期。

## 15. 安全故障演示中心

安全演示遵循固定闭环：

```text
注入故障
 → 检测异常
 → 拦截或停机
 → 显示影响范围
 → 等待恢复
 → 重新自检或回零
 → 恢复运行
```

### 15.1 执行节点离线

不带电拔步进电机相线。通过关闭对应 S3、断开通信、断开使能信号或使用模拟离线开关演示。

检测到 Arm S3 心跳超时后：

- 中止当前任务；
- 禁止后续运动命令；
- 显示受影响的升降、小臂和夹爪；
- 设备恢复后要求重新回零，不能直接续跑。

### 15.2 摄像头离线

程序必须能够发现摄像头断开且不崩溃：

- 尚未抓取物料时停止传送带；
- 已抓取物料时保持夹爪并进入安全位置；
- 摄像头恢复后连续获取若干正常帧，再恢复“视觉就绪”。

### 15.3 非法命令

通过“注入非法命令”按钮生成未定义命令或非法字段组合，显示命令码、风险等级、拒绝结果和累计拦截次数。该测试可以由龙芯安全策略层完成，统一归类为异构安全防护事件。

### 15.4 坐标或参数越界

固定演示坐标：

```text
X=400 mm，Y=0 mm
```

界面显示目标半径 550 mm、允许最大半径 500 mm、目标不可达、禁止下发和机械臂保持静止。另准备一个 Z 轴超范围案例作为备用。

### 15.5 机械限位

通过低速回零或专用测试开关触发限位，不允许高速撞击：

```text
升降上限位触发
危险方向：继续向上
处理结果：立即停止
当前仅允许向下退出
```

### 15.6 急停

急停具有最高优先级，应终止所有运动、关闭推杆并锁定新的动作命令。解除急停后必须由人工确认，并根据当前状态决定是否重新回零。

所有故障注入按钮均需二次确认，并只允许在预设的安全姿态或测试条件下使用。

## 16. 统一软件结构

### 16.1 全局状态模型

建立唯一的系统状态，供所有 Qt 页面读取：

```cpp
struct SystemState {
    bool fpgaOnline;
    bool c3Online;
    bool baseOnline;
    bool armOnline;
    bool cameraOnline;

    bool axisHomed[3];
    bool axisDone[4];
    bool limitTriggered[3];
    bool emergencyStop;

    int currentCommandSeq;
    int securityAllowCount;
    int securityBlockCount;
    int lastBlockReason;
};
```

### 16.2 统一运动接口

关节调试、坐标控制和自动分拣最终调用同一组接口：

```cpp
moveJoint(...)
moveCartesian(...)
homeAxes(...)
setGripper(...)
moveConveyor(...)
emergencyStop(...)
```

安全检查集中在统一接口中执行，避免某个页面绕过检查。

### 16.3 控制数据与展示数据分离

```cpp
RobotControlState controlState;  // 回零、目标位置、限位、到位、故障
RobotDisplayState displayState;  // 角度动画、电流、电压、温度和趋势
```

展示数据不参与运动学、限位判断和安全决策。

## 17. 开发阶段规划

### 第一阶段：基础状态与统一接口

- 建立 `SystemState`；
- 统一运动、急停和回零接口；
- 将控制状态与展示状态分离；
- 统一安全日志接口。

### 第二阶段：坐标控制

- 正逆运动学；
- 工作空间和关节角限幅；
- 目标角度转换为 32 位脉冲；
- 当前软件位置管理；
- 坐标检查、姿态预览和执行。

### 第三阶段：限位、回零与最小反馈

- 三轴限位输入；
- 分阶段回零；
- S3 返回在线、限位、到位、故障和序号；
- 删除或停用高频电流、电压、温度和编码器轮询。

### 第四阶段：自动分拣状态机

- 将现有固定延时流程迁移为统一状态机；
- 为每个状态设置完成条件和超时；
- 实现暂停、终止、急停和故障恢复。

### 第五阶段：答辩界面

- 两个一级入口和六个页面；
- 设备拓扑、流程条和红黄绿状态；
- 实时生产日志和安全日志；
- 分类计数和周期统计；
- 展示数据动画。

### 第六阶段：安全演示与联调

- 坐标越界；
- 非法命令；
- 摄像头离线与自动重连；
- 执行节点离线；
- 机械限位；
- 急停和恢复流程；
- 固化答辩演示脚本。

## 18. 推荐答辩流程

整体控制在 5～7 分钟：

1. 约 30 秒介绍龙芯、FPGA、C3、S3 和机械层架构；
2. 约 2 分钟完整运行一次或多次颜色分拣；
3. 约 1 分钟展示坐标输入、逆运动学结果和目标姿态；
4. 约 2 分钟演示坐标越界、非法命令、摄像头或执行节点离线；
5. 约 1 分钟展示故障恢复、重新回零和生产统计。

演示顺序必须先证明系统可以正常运行，再证明故障情况下能够安全停止和恢复。
